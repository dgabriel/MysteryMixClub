import React from 'react';
import ReactDOM from 'react-dom/client';
import { MusicProof } from './MusicProof';
import '../index.css';
import './proof.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode><MusicProof /></React.StrictMode>,
);
